# Network_Analyzer-mini-project-
I built a Python tool that actively measures five network metrics: IP masking, ping latency, jitter, DNS resolution, and DNS leaks - before and after enabling a VPN. It quantifies the privacy vs. performance trade-off and specifically checks for DNS leaks, which can expose browsing behaviour even when traffic is encrypted.
